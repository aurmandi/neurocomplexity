# Reproducible install lock for neurocomplexity 1.1.0.
#
# Pinned to the versions the green CI matrix resolved on Linux / CPython 3.12
# (ubuntu-22.04, x86_64) on 2026-05-29. Use this file to install the exact
# dependency set the CI test run validated:
#
#     pip install -r requirements-lock.txt
#     pip install --no-deps -e .
#
# Re-generate after dependency bumps with:
#
#     pip install pip-tools
#     pip-compile --output-file=requirements-lock.txt \
#                 --extra=nwb --extra=viz --extra=dev pyproject.toml
#
# All bounds in pyproject.toml remain authoritative; this file is the
# *concrete* resolution snapshot used by the `lock` CI job to confirm that
# the snapshot still passes the test suite.

# --- core runtime ---------------------------------------------------------
numpy==1.26.4
scipy==1.14.0
pandas==2.2.2
tqdm==4.66.4
joblib==1.4.2

# --- nwb extra ------------------------------------------------------------
pynwb==3.1.3
h5py==3.13.0
hdmf==4.1.2

# --- viz extra ------------------------------------------------------------
matplotlib==3.9.2
plotly==6.7.0
dash==4.1.0

# --- dev extra ------------------------------------------------------------
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.15

# --- pinned transitives (top consumers of each are documented above) -----
patsy==1.0.1
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
tzdata==2025.2
contourpy==1.2.1
cycler==0.12.1
fonttools==4.55.0
kiwisolver==1.4.5
packaging==24.2
pillow==11.0.0
pyparsing==3.2.0
Flask==3.1.1
Werkzeug==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.4
blinker==1.9.0
nbformat==5.10.4
narwhals==1.16.0
typing_extensions==4.12.2
